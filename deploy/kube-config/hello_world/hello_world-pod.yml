apiVersion: v1
kind: Pod
metadata:
  name: hello-world
spec:
  containers:
    - image: corysmith/hello_world
      name: hello-world
      ports:
        - containerPort: 80
          name: http
          protocol: TCP
