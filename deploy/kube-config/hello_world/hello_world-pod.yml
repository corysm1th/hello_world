apiVersion: v1
kind: Pod
metadata:
  name: hello-world
spec:
  containers:
    - image: corysmith/hello_world
      name: hello-world
      livenessProbe:
        httpGet:
          path: /
          port: 80
      ports:
        - containerPort: 80
          name: http
          protocol: TCP
